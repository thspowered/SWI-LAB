[project]
name = "swilab"
version = "0.2.0"
description = "SWI-LAB - rezervacny system laboratornych pristrojov"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "pydantic-settings>=2.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "httpx>=0.28",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Testy nachadzaju balik aj vtedy, ked editable install nefunguje.
# (Na macOS staci, aby mal .pth subor v site-packages flag "hidden" -
# CPython skryte .pth zamerne preskakuje a import swilab tichu spadne.)
pythonpath = ["src"]
